# Profile — `{domain}` · Plan

Fill every `{TODO}`. *(must)* slots are gated by GATE-0. See [`../CONTRACT.md`](../CONTRACT.md).

## Design catalog *(must)*
{TODO: the shapes tickets route against in this domain — the patterns/precedent the architect matches a
ticket to. If none yet: "novel — author fresh, capture after."}

## Tiering signals *(must)*
{TODO: what makes a ticket agent-ship vs agent-draft vs human-only here — the risk boundaries specific to
this domain (e.g. "anything crossing the isolation boundary is human-only until the isolation test
exists").}
