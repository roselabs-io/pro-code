# Copy to .env (git-ignored) and fill. Never commit real secrets.
DATABASE_URL=postgresql+asyncpg://blog:blog@localhost:5432/blog
JWT_SECRET=change-me-to-a-random-32-byte-plus-secret
JWT_ALG=HS256
ACCESS_TOKEN_TTL_SECONDS=3600
# Wired at deploy (M7) — roselabs Resend account:
RESEND_API_KEY=
