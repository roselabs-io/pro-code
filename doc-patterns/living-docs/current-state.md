# {{project_name}} — Current State

> Where things actually are **now**. The first doc a fresh agent (or teammate) reads to orient.
> Memory, not documentation — kept current every ticket. **Stale here poisons the next run.**
> Pulled from `doc-patterns/living-docs/current-state.md`. Graded by the docs-currency grader.

## What's built

The capabilities that actually work today — not the plan, the reality.

- {TODO: e.g., "Workspace CRUD — create/rename/delete, tenant-scoped. Shipped T2."}

## What's in flight

Tickets currently being worked, and by whom.

- {TODO: e.g., "T5 tenant isolation — guard applied on reads, writes pending."}

## Known gaps / not-yet-built

The honest edge of the build — what a fresh agent must NOT assume exists.

- {TODO: e.g., "No rate limiting yet. No audit log. Billing stubbed."}

## How to run it

The minimum to bring the system up and exercise it locally.

- {TODO: e.g., "`just up` / `just down` → API on :8000. Tests: `just test`. Full gate: `just gate`."}
